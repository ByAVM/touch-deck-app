import { defineMessages } from "react-intl";

export const appMessages = defineMessages({
    emptyTitle: {
        id: "pages.app.emptyTitle",
        defaultMessage: 'Select panel from menu'
    }
})