import { defineMessages } from "react-intl";

export const clientHooksMessages = defineMessages({
  clientNotInitialized: {
    id: "features.client.hooks.useClient.clientNotInitialized",
    defaultMessage: "Client not initialized",
  },
});
