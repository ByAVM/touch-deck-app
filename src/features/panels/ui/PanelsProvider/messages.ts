import { defineMessages } from "react-intl";

export const panelsUiMessages = defineMessages({
    closeButton: {
        id: 'features.panels.ui.PanelsProvider.closeButton',
        defaultMessage: 'Close'
    }
})